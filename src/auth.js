(() => { const SESSION_KEY = 'ro-platform-session'; const session = () => JSON.parse(localStorage.getItem(SESSION_KEY) || 'null'); const signIn = ({ name = 'Demo Administrator', role = 'admin' } = {}) => { const value = { name, role }; localStorage.setItem(SESSION_KEY, JSON.stringify(value)); return value; }; const signOut = () => localStorage.removeItem(SESSION_KEY); const isAdmin = () => session()?.role === 'admin'; window.ROAuth = { session, signIn, signOut, isAdmin }; })();
